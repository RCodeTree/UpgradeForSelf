#include <stdio.h>
int main()
{
	/* 
	getchar() 函数：
		接收字符数据，在输入时也会接收换行
	*/


	/*
	char a, b, c;

	a = getchar();
	b = getchar();
	c = getchar();

	putchar(a);
	putchar(b);
	putchar(c);
	putchar('\n');
	*/

	putchar(getchar());
	putchar(getchar());
	putchar(getchar());
	putchar('\n');

	return 0;
}
