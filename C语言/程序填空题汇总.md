# C语言程序填空题题目与程序对应汇总

## 第 1 题
程序通过定义学生结构体变量，存储了学生的学号、姓名和三门课的成绩。所有学生数据均以二进制方式输出到文件中。函数fun的功能是重写形参filename所指文件中最后一个学生的数据，即用新的学生数据覆盖该学生原来的数据，其他学生的数据不变。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun(char *filename, STU n)
{
    FILE *fp;
    fp = fopen(____, "rb+");
    fseek(____, ‐(long)sizeof(STU), SEEK_END);
    fwrite(&n, sizeof(STU), 1, ____);
    fclose(fp);
}
```

---

## 第 2 题
给定程序中，函数m的功能是：计算形参x所指数组中N个数的平均值(规定所有数均为正数)作为函数值返回，并将大于平均值的数放在形参y所指数组中。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
#include <stdlib.h>
#include <stdio.h>
#define N 10
double fun(double x[],double *y)
{
    int i,j; double av;
    av=____;
    for (i=0; i<N; i++)
        av=av+____;
    for (i=j=0; i<N; i++)
        if (x[i]>av) y[____]= x[i];
    y[j]=-1;
    return av;
}
```

---

## 第 3 题
给定程序中，函数m的功能是：计算x所指数组中N个数的平均值(规定所有数均为正数)，平均值通过形参返回给主函数，将小于平均值且最接近平均值的数作为函数值返回，并在主函数中输出。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
double fun(double x[],double *av)
{
    int i,j;  double d,s;
    s=0;
    for(i=0; i<N; i++)  s = s + x[i];
    ____=s/N;
    d=32767;
    for(i=0; i<N; i++)
        if(x[i]<*av && *av - x[i]<=d){
            d=*av-x[i];
            j= ____; 
        }
    return  ____;
}
```

---

## 第 4 题
给定程序中，函数fun的功能是：计算形参x所指数组中N个数的平均值(规定所有数均为正数)，将所指数组中大于平均值的数据移至数组的前部，小于等于平均值的数据移至所指数组的后部，平均值作为函数值返回，在主函数中输出平均值和移动后的数据。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
double fun(double *x)
{
    int i, j;   double s, av, y[N];
    s=0;
    for(i=0; i<N; i++) s=s+x[i];
    av= ____; //
    for(i=j=0; i<N; i++)
        if (x[i]>av) {
            y[____]=x[i]; //
            x[i]=-1;
        }
    for(i=0; i<N; i++)
        if (x[i]!= ____ ) y[j++]=x[i]; //
    for(i=0; i<N; i++) x[i] = y[i];
    return av;
}
```

---

## 第 5 题
给定程序中，函数fun的功能是：计算形参x所指数组中N个数的平均值(规定所有数均为正数)，将所指数组中小于平均值的数据移至数组的前部，大于等于平均值的数据移至所指数组的后部，平均值作为函数值返回，在主函数中输出平均值和移动后的数据。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
double fun(double *x)
{
    int i, j; 
    double av, y[N];
    av=0;
    for(i=0; i<N; i++)
        av += ____;
    for(i=j=0; i<N; i++)
        if (x[i]<av) {
            y[j]=x[i]; 
            x[i]=-1;
            ____;
        }
    i=0;
    while(i<N)
    {
        if( x[i]!= -1 ) 
            y[j++]=x[i];
        ____;
    }
    for(i=0; i<N; i++)
        x[i] = y[i];
    return av;
}
```

---

## 第 6 题
给定程序中，函数m的功能是将a和b所指的两个字符串分别转换成面值相同的整数，并进行相加作为函数值返回。规定字符串中只含9个以下数字字符。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
long ctod( char *s )
{
    long d=0;
    while(*s)
    {
        if(isdigit( *s)) {
            d=d*10+*s- ____; //
            ____;
            s++;
        }
        else {
            s++;
        }
    }
    return d;
}
long fun( char *a, char *b )
{
    return ____; //
}
```

---

## 第 7 题
给定程序中，函数m的功能是：调用随机函数产生20个互不相同的整数放在形参a所指数组中(此数组在主函数中已置0)。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun( int *a)
{
    int i, x, n=0;
    x=rand()%20;
    while (n<____) //
    {
        for(i=0; i<n; i++ )
            if (x==a[i] )
                ____; //
        if (i==____) //
            { a[n]=x; n++; }
        x=rand()%20;
    }
}
```

---

## 第 8 题
给定程序中，函数m的功能是：找出N×N矩阵中每列元素中的最大值，并按顺序依次存放于形参b所指的一维数组中。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun(int (*a)[N], int *b)
{ 
    int i,j;
    for(i=0; i<N; i++) {
    	b[i]=____; //
    	for(j=1; j<N; j++)
    		if(b[i] ____ a[j][i]) //
    			b[i]=a[j][i];
    }
}
main()
{ 
    int x[N][N]={ {12,5,8,7},{6,1,9,3},{1,2,3,4},{2,8,4,3} },y[N],i,j;
    printf("\nThe matrix :\n");
    for(i=0;i<N; i++)
    { 
        for(j=0;j<N; j++) printf("%4d",x[i][j]);
        printf("\n");
    }
    fun(____); //
    printf("\nThe result is:");
    for(i=0; i<N; i++) printf("%3d",y[i]);
    printf("\n");
}
```

---

## 第 9 题
给定程序中，函数m的功能是建立一个N×N的矩阵。矩阵元素的构成规律是：最外层元素的值全部为1；从外向内第2层元素的值全部为2；第3层元素的值全部为3，……依此类推。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun(int (*a)[N]) //
{ 
    int i,j,k,m;
    if(N%2==0) m=N/2;
    else m=N/2+1;
    for(i=0; i<m; i++) {
    	for(j=____; j<N-i; j++) //
    		a[i][j]=a[N-1-i][j]=i+1;
    	for(k=i+1; k<N-i; k++)
    		a[k][i]=a[k][N-i-1]=____; //
    }
}
```

---

## 第 10 题
给定程序中，函数m的功能是：判定形参a所指的N×N(规定N为奇数)矩阵是否是“幻方”，若是，函数返回值为1；若不是，函数返回值为0。“幻方”的判定条件是：矩阵每行、每列、主对角线及反对角线上元素之和都相等。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
int fun(int (*a)[N])
{ 
    int i,j,m1,m2,row,colum;
	m1=m2=0;
	for (i=0; i<N; i++)
	{ j=N-i-1; m1+=a[i][i]; m2+=a[i][j]; }
	if(m1!=m2) return 0;
	for(i=0; i<N; i++) {
		row=colum= ____; 
		for(j=0; j<N; j++)
		{ row+=a[i][j]; colum+=a[j][i]; }
		if( (row!=colum) ____ (row!=m1) ) return 0; //
	}
	return ____; //
}
```

---

## 第 11 题
给定程序中，函数m的功能是：用指针指向要调用的函数，并进行调用。规定在【2】处使指针指向f1，在【3】处使指针指向f2。当调用正确时，程序输出：x1=5.000000, x2=3.000000, x1*x1+x1*x2=40.000000。

### 程序
```c
double f1(double x) { return x*x; }
double f2(double x, double y) { return x*y; }
double fun(double a, double b)
{
	____ (*f)(); //
	double r1, r2;
	f = ____ ; //
	r1 = f(a);
	f = ____ ; //
	r2 = (*f)(a, b);
	return r1 + r2;
}
```

---

## 第 12 题
给定程序中，函数fun的功能是将带头结点的单向链表结点数据域中的数据从小到大排序。
即若原链表为：1->3->2->4->5，则排序后为：1->2->3->4->5。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun(NODE *h)
{
    NODE *p, *q; int t;
	p = ____; //
	while (p) {
		q = ____; //
		while (q) {
			if (p->data ____ q->data) //
			{ t = p->data; p->data = q->data; q->data = t; }
			q = q->next;
		}
		p = p->next;
	}
}
```

---

## 第 13 题
给定程序中，函数fun的功能是：将形参s所指字符串中的所有数字字符顺序前移，其他字符顺序后移，处理后新字符串的首地址作为函数值返回。
例如，若s所指字符串为"a1b2c3d4e5f6"，则处理后新字符串为"123456abefc"。  

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
char *fun(char *s)
{
    int i, j, k, n; char *p, *t;
    n=strlen(s)+1;
    t=(char*)malloc(n*sizeof(char));
    p=(char*)malloc(n*sizeof(char));
    j=0; k=0;
    for(i=0; i<n; i++)
    { 
        if(isdigit(s[i])) { 
            p[____]=s[i]; 
        } 
        else
        { 
            t[k]=s[i]; 
            k++; 
        }
    }
    for(i=0; i<____; i++) 
        p[j+i] = t[i]; 
    p[j+k]=0;
    return ____; 
}
```

---

## 第 14 题
给定程序中，函数fun的功能是：将a所指的M×N矩阵中第k行的元素与第0行元素交换。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun(int (*a)[N], int ____ ) //
{
    int i,temp ;
    for(i = 0 ; i < ____ ; i++) //
    {
        temp=a[0][i] ;
        a[0][i] = ____ ; //
        a[k][i] = temp ;
    }
}
```

---

## 第 15 题
给定程序中，函数fun的功能是：将a所指的M×N矩阵中第k列的元素左移到第0列，第k列以后的每列元素依次左移，原来左边的各列依次绕到右边。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun(int (*a)[N],int k)
{
    int i,j,p,temp;
    for(p=1; p<= ____; p++) 
        for(i=0; i<M; i++)
        {
            temp=a[i][0];
            for(j=0; j< ____ ; j++) 
                a[i][j]=a[i][j+1]; 
            a[i][N-1]= ____; 
        }
}
```

---

## 第 16 题
给定程序中，函数fun的功能是：在3×4的矩阵中找出在行上最大、在列上最小的那个元素，若没有符合条件的元素则输出相应信息。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun(int (*a)[N])
{
    int i=0,j,find=0,rmax,c,k;
    while ((i<M) && (!find))
    {
        rmax=a[i][0]; c=0;
        for(j=1; j<N; j++)
            if(rmax<a[i][j]) { 
                rmax=a[i][j]; c= ____ ; //
            }
        find=1; k=0;
        while(k<M && find) { 
            if (k!=i && a[k][c]<=rmax) 
                find= ____ ; //
            k++;
        }
        if(find) 
            printf("find: a[%d][%d]=%d\n",i,c,a[i][c]);
        ____ ; //
    }
    if(!find) 
        printf("not found\n");
}
```

---

## 第 17 题
给定程序中，函数fun的功能是：把形参s所指字符串中最右边的n个字符复制到形参t所指字符数组中，形成一个新串。若s所指字符串的长度小于n，则将整个字符串复制到形参t所指字符数组中。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun(char *s, int n, char *t)
{
    int len,i,j=0;
    len=strlen(s);

    if(n>=len) 
        strcpy(____); //
    else { 
        for(i=len-n; i<=len-1; i++) 
            t[j++]= ____ ; //
        t[j]= ____ ; //
    }
}
```

---

## 第 18 题
给定程序中，函数fun的功能是：判断形参s所指字符串是否是“回文”(Palindrome)。若是，函数返回值为1；不是，函数返回值为0。“回文”是正读和反读都一样的字符串(不区分大小写字母)。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
int fun(char *s)
{
    char *lp,*rp;
    lp= ____ ; //lp指向起始地址，rp指向结尾地址
    rp=s+strlen(s)-1;
    while((toupper(*lp)==toupper(*rp)) && (lp<rp) ) { 
        lp++; rp ____; } //
    if(lp<rp) ____ ; //
    else return 1;
}
```

---

## 第 19 题
给定程序中，函数fun的功能是：计算出形参s所指字符串中包含的单词个数，作为函数值返回。为便于统计，规定各单词之间用空格隔开。
例如，若s所指字符串为"a b c d e f"，则单词个数为6。
请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
int fun(char *s)
{
    int n=0, flag=0;
    while(*s!='\0')
    { 
        if(*s!=' ' && flag==0) { 
            ____ ;
            flag=1;
        } //
        if (*s==' ') 
            flag= ____ ; //
        ____ ; //
    }
    return n;
}
```

---

## 第 20 题
给定程序中，函数fun的功能是：将形参s所指字符串中所有ASCII码值小于97的字符存入形参t所指字符数组中，形成一个新串，并统计所存字符的个数作为函数值返回。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
int fun(char *s, char *t)
{
    int n=0;
    while (*s)
    {
        if(*s < 97) 
        {
            *(t+n)= ____ ; 
            n++;
        }
        ____ ;
    }
    *(t+n)=0;
    return ____ ;
}
```

---

## 第 21 题
给定程序中，函数fun的功能是：将形参s所指字符串中的数字字符转换成对应的数值，计算出这些数值的累加和作为函数值返回。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
int fun(char *s)
{
    int sum=0;
    while(*s) {
        if(isdigit(*s)) 
            sum+= *s-____; //
        //'\0'字符对应的ASCII码值是48
        ____; //
    }
    return ____ ; //
}
```

---

## 第 22 题
给定程序中，函数fun的功能是：找出形参s所指字符串中出现频率最高的字母(不区分大小写)，并统计出其出现的次数。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun(char *s)
{
    int k[26]={0},i,n,max=0;  char ch;
    while(*s){
        if(isalpha(*s)) { 
            ch=tolower(____); //
            n=ch-'a';
            k[n]+=____ ; //
        }
        s++;
        if(max<k[n]) 
            max= ____ ; //
    }
    
    printf("\nAfter count :\n");
    for(i=0; i<26;i++)
        if (k[i]==max) 
            printf("\nletter '%c' : %d times\n",i+'a',k[i]);
}
```

---

## 第 23 题
给定程序中，函数fun的功能是：利用指针数组对形参ss所指字符串数组中的字符串按由长到短的顺序排序，并输出排序结果。ss所指字符串数组中共有N个字符串，且串长小于M。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun(char (*ss)[M])
{
    char *ps[N],*tp;  int i,j,k;
    for(i=0; i<N; i++) 
        ps[i]=ss[i];
    for(i=0; i<N-1; i++) {
        k= ____ ; //
        for(j=i+1; j<N; j++)
            if(strlen(ps[k]) < strlen(____) ) 
                k=j; //
        tp=ps[i]; 
        ps[i]=ps[k]; 
        ps[k]= ____ ; //
    }
    printf("\nThe string after sorting by length:\n\n");
    for(i=0; i<N; i++) 
        puts(ps[i]);
}
```

---

## 第 24 题
给定程序中，函数fun的功能是：根据形参i的值返回某个函数的值。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
double f1(double x)
{ return x*x; }
double f2(double x, double y)
{ return x*y; }
____ fun(int i, double x, double y) //
{
    if (i==1)
        return ____(x); //
    else
        return ____(x, y); //
}
```

---

## 第 25 题
程序通过定义学生结构体变量，存储了学生的学号、姓名和三门课的成绩。所有学生数据均以二进制方式输出到文件中。函数fun的功能是从形参filename所指的文件中读入学生数据，并按照学号从小到大排序后，再用二进制方式把排序后的学生数据输出到filename所指的文件中，覆盖原来的文件内容。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun(char *filename)
{
    FILE *fp;  int i, j;
    STU s[N], t;
    fp = fopen(filename, ____); //
    fread(s, sizeof(STU), N, fp);
    fclose(fp);
    for (i=0; i<N-1; i++)
        for (j=i+1; j<N; j++)
            if (s[i].sno ____ s[j].sno) //
            {
                t = s[i]; 
                s[i] = s[j]; 
                s[j] = t; 
            }
    fp = fopen(filename, "wb");
    ____(s, sizeof(STU), N, fp); //
    fclose(fp);
}
```

---

## 第 26 题
给定程序中，函数fun的功能是：在任意给定的N个正整数中找出按升序排列时处于中间的数。将原数据序列中比该中间数小的数用该中间数替换，位置不变。在主函数中输出处理后的数据序列，并将中间数作为函数值返回。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
int fun(int x[])
{
    int i,j,k,t,mid,b[N];
    for(i=0;i<N;i++) 
        b[i]=x[i];
    for(i=0;i<=N/2;i++)
    {
        k=i;
        for(j=i+1;j<N;j++) 
            if(b[k]>b[j]) 
                k=j;
        if(k != i )
        {
            t=b[i]; 
            b[i]=____; 
            b[k]=t; //
        }
    }
    mid=b[____];
    for(i=0; i<N; i++)
        if(x[i] ____ mid) 
            x[i]=mid;
    return mid;
}
```

---

## 第 27 题
给定程序中，函数fun的功能是：输出a所指数组中的前n个数，要求每行输出5个数。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun( int *a, int n )
{
    int i;
    for(i=0; i<n; i++)
    {
        if(____==0) //
            printf("____"); //
        printf("%d ",____s); //
    }
}
```

---

## 第 28 题
给定程序的主函数中，已给出由结构体构成的链表结点a、b、c、d，各结点的数据域中均存入字符，函数fun()的作用是：将a、b、c三个结点链接成一个单向链表，并输出链表结点中的数据。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun( Q *pa, Q *pb, Q *pc)
{
    Q *p;
    pa->next=____; //
    pb->next=pc;
    p=pa;
    while( p )
    {
        printf(" %c",____); //
        p=____; //
    }
    printf("\n");
}
```

---

## 第 29 题
围绕山顶一圈有N个山洞，编号为0、1、2、3、……、N-1。有一只狐狸和一只兔子在洞中居住。狐狸总想找到兔子并吃掉它，它的寻找方法是先到第一个洞(即编号为0的洞)中找；再隔1个洞，即到编号为2的洞中找；再隔2个洞，即到编号为5的洞中找。下次再隔3个洞，即到编号为9的洞中找；……，若狐狸找一圈，请为兔子指出所有不安全的洞号。程序中用数组a指示每个洞，数组元素的下标即为洞号，数组元素中的值为0时，表示该洞安全，为1时表示该洞不安全。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun( int *a , int n )
{
    int i, t;
    for( i=0; i<n; i++ )
        a[i]=____; //
    i=0;
    ____=1; //
    while( i<n )
    {
        a[i] = 1;
        t++;
        i=____; //
    }
}
```

---

## 第 30 题
给定程序中，函数fun的功能是：统计整型变量m中各数字出现的次数，并存放到数组a中。其中，a[0]存放0出现的次数，a[1]存放1出现的次数，……a[9]存放9出现的次数。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun( int m, int a[10])
{
    int i;
    for( i=0; i<10; i++ )
        ____ = 0; //
    while( m > 0)
    {
        i = ____; //
        a[i]++;
        m = ____; //
    }
}
```

---

## 第 31 题
给定程序中，函数fun的功能是计算下式前n项的和，并作为函数值返回。

$$
S = \frac{1 \times 3}{2^2} - \frac{3 \times 5}{4^2} + \frac{5 \times 7}{6^2} + (-1)^{n-1} \frac{(2n - 1)(2n + 1)}{(2n)^2}
$$

例如，当形参n的值为10时，函数返回：-0.204491。

请在下面画线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
double fun(int n)
{
    int i, k; double s, t;
    s=0;
    k=____; //
    for(i=1; i<=n; i++) {
        t=____; //
        s=s+k*(2*i-1)*(2*i+1)/(t*t);
        k=k*____; //
    }
    return s;
}
```

---

## 第 32 题
下列给定程序中，函数fun的功能是计算下式

$$
s = \frac{1}{2^2} + \frac{3}{4^2} + \frac{5}{6^2} + \cdots + \frac{2 \times n - 1}{(2 \times n)^2}
$$

直到 
$$
\left| \frac{2 \times n + 1}{(2 \times n)^2} \right| \leq 10^{-3}
$$
，并将计算结果作为函数值返回。

例如，若形参e的值为1e-3，函数的返回值为2.985678。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
double fun(double e)
{
    int i;    double s, x;
    s=0;    
	i=____; //
    x=1.0;
    while(x>e){
        ____; //
        x=(2.0*i-1)/((____)*(2.0*i)); //
        s=s+x;
    }
    return s;
}
```

---

## 第 33 题
下列给定程序中，函数fun的功能是：在形参s所指字符串中的每个数字字符之后插入一个*号。例如，形参s所指的字符串为"def35adh3kjsdf7"，执行后结果为"def3*5*adh3*kjsdf7*"。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char *s)
{
    int i, j, n;
    for(i=0; s[i]!='\0'; i++)
        if(s[i]>='0' ____ s[i]<='9') //
        {
            n=0;
            while(s[i+1+n]!=____) n++; //
            for(j=i+n+1; j>i; j--)
                s[j+1] = ____; //
            s[j+1]='*';
            i=i+1;
        }
}
```

---

## 第 34 题
给定程序中，函数fun的功能是将不带头结点的单向链表逆置，即若原链表中从头至尾结点数据依次为2、4、6、8、10，逆置后，从头至尾结点数据依次为10、8、6、4、2。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
____ * fun(NODE *h) //
{
    NODE *p, *q, *r;
    p = h;
    if (p == NULL)
        return NULL;
    q = p->next;
    p->next = NULL;
    while (q)
    {
        r = q->____; //
        q->next = p;
        p = q;
        q = ____; //
    }
    return p;
}
```

---

## 第 35 题
程序通过定义学生结构体变量，存储学生的学号、姓名和三门课的成绩。函数fun的功能是，将形参a中的数据进行修改，把修改后的数据作为函数值返回主函数进行输出。
例如，若传给形参a的数据中学号、姓名和三门课的成绩依次是：10001、"ZhangSan"、95、80、88，修改后的数据应为：10002、"LiSi"、96、81、89。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
____ fun(struct student a) //
{
    int i;
    a.sno = 10002;
    strcpy(____, "LiSi"); //
    for(i=0; i<3; i++) ____ += 1; //
    return a;
}
```

---

## 第 36 题
给定程序中，函数fun的功能是将带头结点的单向链表逆置，即若原链表中从头至尾结点数据依次为2、4、6、8、10，逆置后，从头至尾结点数据依次为10、8、6、4、2。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(NODE *h)
{
    NODE *p, *q, *r;
    p = h->____; //
    if (p==____) return; //
    q = p->next;
    p->next = NULL;
    while (q)
    {
        r = q->next; 
        q->next = p;
        p = q;
        q = ____; //
    }
    h->next = p;
}
```

---

## 第 37 题
下列给定程序中，函数fun的功能是：将形参s所指字符串中下标为奇数的字符取出，并按ASCII码大小递增排序，将排序后的字符存入形参p所指字符数组中，形成一个新串。

例如，形参s所指的字符串为"basnkjikgdfu"，执行后所指字符数组中的字符串应为"adfijkuy"。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char *s, char *p)
{
    int i, j, n, x, t;
    n=0;
    for(i=0; s[i]!='\0'; i++) n++;
    for(i=1; i<n-2; i=i+2) {
        ____; //
        for(j=____+2; j<n; j=j+2) //
            if(s[t]>s[j]) t=j;
        if(t!=i)
        { x=s[i]; s[i]=s[t]; s[t]=x; }
    }
    for(i=1,j=0; i<n; i=i+2, j++) p[j]=s[i];
    p[j]=____; //
}
```

---

## 第 38 题
给定程序中，函数fun的功能是将不带头结点的单向链表结点数据域中的数据从小到大排序。所有原始链表结点数据域从头至尾的数据为0、2、4、8、6，排序后链表结点数据域从头至尾的数据为0、2、4、6、8。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(NODE *h)
{
    NODE *p, *q; int t;
    p = h;
    while (p) {
        q = ____; //
        while (____) //
        {
            if (p->data > q->data)
            {
                t = p->data; p->data = q->data; q->data = t;
            }
            q = q->next;
        }
        p = ____; //
    }
}
```

---

## 第 39 题
给定程序中，函数fun的功能是将形参给定的字符串、整数、浮点数写到文本文件中，再用字符方式从此文本文件中逐个读入并显示在终端屏幕上。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char *s, int a, double f)
{
    ____ fp; //
    char ch;
    fp = fopen("file1.txt", "w");
    fprintf(fp, "%s %d %f\n", s, a, f);
    fclose(fp);
    fp = fopen("file1.txt", "r");
    
    printf("\nThe result :\n\n");
    ch = fgetc(fp);
    while (!feof(____)) //
    {
        putchar(____); //
        ch = fgetc(fp);
    }
    putchar('\n');
    fclose(fp);
}
```

---

## 第 40 题
下列给定程序中，函数fun的功能是：在形参s所指字符串中寻找与参数c相同的字符，并在其后插入一个与之相同的字符，若找不到相同的字符则不做任何处理。

例如，若所指字符串为"abcdc"，c中的字符为'c'，执行后所指字符串为"abccdc"。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char *s, char c)
{
    int i, j, n;
    for(i=0; s[i]!=____ ; i++) //
        if (s[i]==c)
        {
            n=____; //
            while(s[i+1+n]!='\0') n++;
            for(j=i+n+1; j>i; j--) s[j+1]=s[j];
            s[j+1]=____; //
            i=i+1;
        }
}
```

---

## 第 41 题
下列给定程序中，函数fun的功能是：将形参s所指字符串中的所有字母字符顺序前移，其他字符顺序后移，处理后将新字符串的首地址作为函数值返回。

例如，若s所指字符串为"a*d123f4gh5"，处理后新字符串为"adfgh12345"。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
char *fun(char *s)
{ int i, j, k, n;    char *p, *t;
    n=strlen(s)+1;
    t=(char*)malloc(n*sizeof(char));
    p=(char*)malloc(n*sizeof(char));
    j=0; k=0;
    for(i=0; i<n; i++)
    { if(((s[i]>='a')&&(s[i]<='z'))||((s[i]>='A')&&(s[i]<='Z'))) { 
        t[j]=____; j++;} //
        else
        { p[k]=s[i]; k++; }
    }
    for(i=0; i<____; i++) t[j+i]=p[i]; //
    t[j+k]= ____; //
    return t;
}
```

---

## 第 42 题
给定程序中，函数fun的功能是将参数给定的字符串、整数、浮点数写到文本文件中，再用字符串方式从此文本文件中逐个读入，并调用库函数atoi和atof将字符串转换成相应的整数、浮点数然后再显示在屏幕上。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char *s, int a, double f)
{
    ____ fp; //
    char str[100], str1[100], str2[100];
    int a1;    double f1;
    fp = fopen("file1.txt", "w");
    fprintf(fp, "%s %d %f\n", s, a, f);
    ____;
    fp = fopen("file1.txt", "r");
    fscanf(____, "%s%s%s", str, str1, str2); //
    fclose(fp);
    a1 = atoi(str1);
    f1 = atof(str2);
    printf("\nThe result :\n\n%s %d %f\n", str, a1, f1);
}
```

---

## 第 43 题
下列给定程序中，函数fun的功能是：计算N×N矩阵的主对角线元素和反向对角线元素之和，并作为函数值返回。要求先累加主对角线元素中的值，再累加反向对角线元素中的值。

例如，若N=3，有下列矩阵：
```
1 2 3
4 5 6
7 8 9
```

首先累加主对角线元素1、5、9，然后累加反向对角线元素3、5、7，函数返回值为30。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
fun(int t[][N], int n)
{ int i, sum;
    ____; //
    for(i=0; i<n; i++)
        sum+= ____ ; //
    for(i=0; i<n; i++)
        sum+= t[i][n-i-____] ; //
    return sum;
}
```

---

## 第 44 题
下列给定程序中，函数fun的功能是：找出100到x(x≤999)之间各位上的数字之和为15的所有整数，并在屏幕输出；将符合条件的整数的个数作为函数值返回。

例如，当x为500时，各位数字之和为15的整数有：159、168、177、186、195、249、258、267、276、285、294、339、348、357、366、375、384、393、429、438、447、456、465、474、483、492，共有26个。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
int fun(int x)
{ int n, s1, s2, s3, t;
    n=____; //
    t=100;
    while(t<=____) //
    {
        s1=t%10; s2=(t/10)%10; s3=t/100;
        if(s1+s2+s3==15)
        {
            printf("%d ",t);
            n++;
        }
        ____; //
    }
    return n;
}
```

---

## 第 45 题
下列给定程序中，函数fun的功能是：在形参ss所指字符串数组中，将所有串长超过k的字符串中后面的字符删除，只保留前面的k个字符。ss所指字符串数组中共有N个字符串，且串长小于M。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char (*ss)____, int k) //
{ int i=0 ;
    while(i< ____) { //
        ss[i][k]=____; 
        i++; 
    }
}
```

---

## 第 46 题
下列给定程序中，函数fun的功能是：将自然数1～10及其平方根写到名为myfile3.txt的文本文件中，然后再顺序读出显示在屏幕上。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
int fun(char *fname )
{ FILE *fp;  int i,n;
    float x;
    if((fp=fopen(fname, "w"))==NULL) return 0;
    for(i=1;i<=10;i++)
        fprintf(____, "%d %f\n",i,sqrt((double)i)); //
    ____; //
    printf("\nSucceed!\n");
    printf("\nThe data in file :\n");
    if((fp=fopen(____, "r"))==NULL) //
        return 0;
    fscanf(fp,"%d%f",&n,&x);
    while(!feof(fp))
    { printf("%d %f\n",n,x); fscanf(fp,"%d%f",&n,&x); }
    fclose(fp);
    return 1;
}
```

---

## 第 47 题
下列给定程序中，函数fun的功能是：将形参n所指变量中，各位上为偶数的数去掉，剩余的数按原来从高位到低位的顺序组成一个新数，并通过形参指针n传回所指变量。
例如，若输入一个数27638496，则新数为739。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(unsigned long *n)
{ unsigned long x=0, i; int t;
    i=1;
    while(*n)
    { t=*n % ____; //取出该数值的各个位
        if(t%2 != ____) //
        { x=x+t*i; i=i*10; }
        *n =*n /10;
    }
    *n= ____; //
}
```

---

## 第 48 题
下列给定程序中，函数fun的功能是：将N×N矩阵中元素的值按列向右移动1个位置，右边被移出矩阵的元素绕回左边第1列。
例如，N=3，有下列矩阵：
1 2 3
4 5 6
7 8 9
计算结果为
3 1 2
6 4 5
9 7 8

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(int (*t)[N])
{ int i, j, x;
    for(i=0; i<____; i++) //
    {
        x=t[i][____] ; //
        for(j=N-1; j>=1; j--)
            t[i][j]=t[i][j-1];
        t[i][____]=x; //
    }
}
```

---

## 第 49 题
下列给定程序中，函数fun的功能是：把形参a所指数组中的最大值放在a[0]中，接着求出a所指数组中的最小值放在a[1]中，再把a所指数组元素中的次大值放在a[2]中，把a数组元素中的次小值放在a[3]中，以此类推。
例如，若所指数组中的数按顺序排列为：1、4、2、3、9、6、5、8、7，则按规则移动后，数据排列为：9、1、8、2、7、3、6、4、5。形参n中存放所指数组中数据的个数。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(int ____, int n) //
{ int i, j, max, min, px, pn, t;
    for (i=0; i<n-1; i+=____) //
    { max = min = a[i];
        px = pn = i;
        for (j=____; j<n; j++) //
        { if (max < a[j])
            { max = a[j]; px = j; }
            if (min > a[j])
            { min = a[j]; pn = j; }
        }
        if (px != i)
        { t = a[i]; a[i] = max; a[px] = t;
            if (pn == i) pn = px;
        }
        if (pn != i+1)
        { t = a[i+1]; a[i+1] = min; a[pn] = t; }
    }
}
```

---

## 第 50 题
下列给定程序中，函数fun的功能是：在带头结点的单向链表中，查找数据域中值为ch的结点。找到后通过函数值返回该结点在链表中所处的顺序号；若不存在值为ch的结点，函数返回0值。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
int fun( SLIST *h, char ch)
{ SLIST *p;     int n=0;
    p=h->next;
    while(p!= ____) //
    {
        n++;
        if(p->data==ch) return ____; //
        else p=p->next;
    }
    return 0;
}
main()
{
    SLIST *head;  int k;  char ch;
    char a[N]={'m','p','g','a','w','x','r','d'};
    head=creatlist(a);
    outlist(head);
    printf("Enter a letter:");
    scanf("%c",&ch);
    k=fun(____); //
    if (k==0)  printf("\nNot found!\n");
    else      printf("The sequence number is : %d\n",k);
}
```

---

## 第 51 题
下列给定程序中，函数fun的功能是：将形参n中各位上为偶数的数取出，并按原来从高位到低位的顺序组成一个新数作为函数值返回。
例如，从主函数输入一个整数27638496，则函数返回值为26846。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
unsigned long fun(unsigned long n)
{ unsigned long x=0, s, i; int t;
    s=n;
    i= ____; //
    while( ____ ) //
    {
        t=s%10;
        if(t%2==0){
            x=x+t*i; i= ____; //
        }
        s=s/10;
    }
    return x;
}
```

---

## 第 52 题
下列给定程序中，函数fun的功能是：把形参a所指数组中的偶数按原顺序依次存放到a [0]、a [1]、a [2] …中，把奇数从数组中删除，偶数的个数通过函数值返回。

例如:若所指数组中的数据最初排列为：9,1,4,2,3,6,5,8,7,删除奇数后新数组中的数据为：4,2,6,8,返回值为4。

请在程序的下划线处填入正确的内容并将下划线删除,使程序得出正确的结果。

### 程序
```c
int fun(int a[], int n)
{ int i,j;
  j = 0;
  for (i=0; i<n; i++)
    if (____ == 0) {
      ____ = a[i]; j++; 
    }
  return ____;
}
```

---

## 第 53 题
下列给定程序中，函数fun的功能是：统计带头结点的单向链表中结点的个数，并存放在形参n所指的存储单元中。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void outlist(SLIST *);
void fun( SLIST *h, int *n)
{ SLIST *p;
  ____=0; 
  p=h->next;
  while(p)
  { (*n)++;
    p=p->____; 
  }
}
main()
{ SLIST *head;
  int a[N]={12,87,45,32,91,16,20,48}, num;
  head=creatlist(a); outlist(head);
  fun(____, &num); 
  printf("\nnumber=%d\n",num);
}
```

---

## 第 54 题
下列给定程序的功能是调用fun函数建立班级通讯录。通讯录中记录每位学生的编号、姓名和电话号码，班级人数和学生信息从键盘读入，每个人的信息作为一个数据块写到名为myfile5.dat的二进制文件中。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
int fun(____ *std) 
{
  ____ *fp;  int i; 
  if((fp=fopen("myfile5.dat","wb"))==NULL)
    return(0);
  printf("\nOutput data to file !\n");
  for(i=0; i<N; i++)
    fwrite(&std[i], sizeof(STYPE), 1, ____); 
  //fwrite(buffer,size,count,fp);
  fclose(fp);
  return (1);
}
```

---

## 第 55 题
程序通过定义学生结构体数组，存储若干名学生的学号、姓名和三门课的成绩。函数fun的功能是：将存放学生数据的结构体数组按姓名的字典序(从小到大)排序。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
void fun(struct student a[], int n)
{
  ____ t; 
  int  i, j;
  for (i=0; i<____; i++) 
    for (j=i+1; j<n; j++)
      if (____ > 0) 
      { t = a[i]; a[i] = a[j]; a[j] = t; }
}
```

---

## 第 56 题
人员的记录由编号和出生年、月、日组成，N名人员的数据已在主函数中存入结构体数组std中，且编号唯一。函数fun的功能是：找出指定编号人员的数据，作为函数值返回，由主函数输出，若指定编号不存在，返回数据中的编号为空串。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
____ fun(STU *std, char *num)
{ int i; STU a={"",9999,99,99};
    for (i=0; i<N; i++)
        if ( strcmp(____,num)==0)
            return (____);
    return a;
}
```

---

## 第 57 题
人员的记录由编号和出生年、月、日组成，N名人员的数据已在主函数中存入结构体数组std中。函数fun的功能是：找出指定出生年份的人员，将其数据放在形参k所指的数组中，由主函数输出，同时由函数值返回满足指定条件的人数。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
int fun(STU *std, STU *k, int year)
{ int i,n=0;
    for(i=0; i<N; i++)
        if(____==year) //
            k[n++]= ____; //
    return (____); //
}
```

---

## 第 58 题
给定程序通过定义并赋初值的方式，利用结构体变量存储了一名学生的学号、姓名和三门课的成绩。函数modify的功能是将该学生的各科成绩都乘以一个系数a。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void modify(____ *ss,float a) //
{ int i;
    for(i=0; i<3; i++)
        ss->____ *=a;
}
main( )
{ STU std={ 1,"Zhanghua",76.5,78.0,82.0 };
    float a;
    printf("\nThe original number and name and scores :\n");
    show(std);
    printf("\nInput a number : "); scanf("%f",&a);
    modify(____,a); //
    printf("\nA result of modifying :\n");
    show(std);
}
```

---

## 第 59 题
给定程序中，函数fun的功能是：将形参std所指结构体数组中的三个元素按num成员进行升序排列。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(PERSON ____) //
{ ____ temp; //
    if(std[0].num>std[1].num)
    { temp=std[0]; std[0]=std[1]; std[1]=temp; }
    if(std[0].num>std[2].num)
    { temp=std[0]; std[0]=std[2]; std[2]=temp; }
    if(std[1].num>std[2].num)
    { temp=std[1]; std[1]=std[2]; std[2]=temp; }
}
main()
{ PERSON std[]={ 5,"Zhanghu",2,"WangLi",6,"LinMin" };
    int i;
    fun(____); //
    printf("\nThe result is :\n");
    for(i=0; i<3; i++)
        printf("%d,%s\n",std[i].num,std[i].name);
}
```

---

## 第 60 题
给定程序中，函数fun的功能是：将形参std所指结构体数组中年龄最大者的数据作为函数值返回，并在main函数中输出。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
STD fun(STD std[], int n)
{ STD max; int i;
    max= ____; //
    for(i=1; i<n; i++)
        if(max.age< ____) max=std[i]; //
    return max;
}
main( )
{ STD std[5]={"aaa",17,"bbb",16,"ccc",18,"ddd",17,"eee",15 };
    STD max;
    max=fun(std,5);
    printf("\nThe result: \n");
    printf("\nName : %s, Age : %d\n", ____,max.age); //
}
```

---

## 第 61 题
下列给定程序中，函数fun的功能是：计算如下公式前n项的和并作为函数值返回。
$$
s=1+\frac{3}{2^2}+\frac{5}{3^2}+\frac{7}{4^2}+\cdots+\frac{(2\times n-1)}{n^2}+\frac{(2\times n+1)}{(n+1)^2}
$$
例如，当形参的n值为10时，函数数值为9.612558。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
double fun(int n)
{ int i; double s, t;
    s=____; //
    for(i=1; i<=____; i++) //
    { t=2.0*i;
        s=s+(2.0*i-1)*(2.0*i+1)/____; //
    }
    return s;
}
```

---

## 第 62 题
给定程序中，函数fun的功能是：统计形参s所指的字符串中数字字符出现的次数，并存放在形参t所指的变量中，最后在主函数中输出。
例如，若形参s所指的字符串为"odc555g6dji92k83g7q7"，则输出结果为4。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char *s, int *t)
{ int i, n;
    n=0;
    for(i=0; ____ !=0; i++) //
        if(s[i]>='0'&&s[i]<=____) n++; //
    ____;
}
```

---

## 第 63 题
下列给定程序中，函数fun的功能是：把形参a所指数组中的奇数按原顺序依次存放到a[0]、a[1]、a[2]、……中，把偶数从数组中删除，奇数的个数通过函数值返回。
例如，若所指数组中的数据最初排列为：9、1、4、2、3、6、5、8、7，则删除偶数后a所指数组中的数据为：9、1、3、5、7，返回值为5。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
int fun(int a[], int n)
{ int i,j;
    j = 0;
    for (i=0; i<n; i++)
        if (a[i]%2== ____ ) //
        {
            a[j] = a[i]; 
			____ ; //
        }
    return ____ ;
}
```

---

## 第 64 题
下列给定程序中，函数fun的功能是：将形参n中各位上为偶数的数取出，并按原来从高位到低位相反的顺序组成一个新数，作为函数值返回。
例如，输入一个整数27638496，则函数返回值为64862。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
unsigned long fun(unsigned long n)
{ unsigned long x=0; int t;
    while(n)
    {
        t=n%10;
        if(t%2== ____ ) //
            x= ____ +t; //
        n= ____ ; //
    }
    return x;
}
```

---

## 第 65 题
下列给定程序中，函数fun的功能是：把形参a所指数组中的最小值放在元素a[0]中，接着把a所指数组中的最大值放在a[1]元素中；再把a所指数组元素中的次小值放在a[2]中，把a所指数组元素中的次大值放在a[3]，以此类推。
例如，若所指数组中的数据按顺序排列为：9、1、4、2、3、6、5、8、7，则按规则移动后，数据排列为：1、9、2、8、3、7、4、6、5，形参n中存放所指数组中数据的个数。
规定fun函数中的max存放当前所找的最大值，px存放当前所找最大值的下标。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(int a[], int n)
{ int i,j, max, min, px, pn, t;
    for (i=0; i<n-1; i+=2)
    {
        max = min = ____ ; //
        px = pn = i;
        for (j=i+1; j<n; j++) { 
            if (max< ____ ) //
            { max = a[j]; px = j; }
            if (min> ____ ) //
            { min = a[j]; pn = j; }
        }
        if (pn != i)
        { t = a[i]; a[i] = min; a[pn] = t;
            if (px == i) px =pn;
        }
        if (px != i+1)
        { t = a[i+1]; a[i+1] = max; a[px] = t; }
    }
}
```

---

## 第 66 题
下列给定程序中，函数fun的功能是进行数字字符转换。若形参ch中是数字字符'0'～'9'，则将'0'转换成'9'，'1'转换成'8'，'2'转换成'7'，……，'9'转换成'0'；若是其它字符则保持不变，并将转换后的结果作为函数值返回。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
____ fun(char ch) //
{
    if (ch>='0' && ____) //
        return '9'- (ch-____); //
    return ch ;
}
```

---

## 第 67 题
下列给定程序中，函数fun的功能是：求ss所指字符串数组中长度最短的字符串所在的行下标，作为函数值返回，并把其串长放在形参n所指的变量中。ss所指字符串数组中共有M个字符串，且串长小于N。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
int fun(char (*ss)[N], int *n)
{ int i, k=0, len= N;
    for(i=0; i< ____; i++) //
    {
        len=strlen(ss[i]);
        if(i==0) *n=len;
        if(len ____ *n) //
        {
            *n=len;
            k=i;
        }
    }
    return( ____ ); //
}
```

---

## 第 68 题
下列给定程序中，函数fun的功能是：将所指字符串中的所有数字字符移到所有非数字字符之后，并保持数字字符串和非数字字符串原有的次序。
例如，s所指的字符串为"def35adf3kjsdi7"，执行后结果为"defadfkjsdi3537"。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char *s)
{ int i, j=0, k=0;    char t1[80], t2[80];
    for(i=0; s[i] != '\0'; i++)
        if(s[i]>='0' && s[i]<='9')
        {
            t2[j]=s[i]; 
			____ ; //
        }
        else t1[k++]=s[i];
    t2[j]=0; t1[k]=0;
    for(i=0; i<k; i++) ____ ; //
    for(i=0; i<____; i++) s[k+i]=t2[i];
}
```

---

## 第 69 题
下列给定程序中已建立一个带头结点的单向链表，链表中的各结点按结点数据域中的数据递增有序链接。函数fun的功能是：把形参x的值放入一个新结点并插入链表中，使插入后各结点数据域中的数据仍保持递增有序。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun( SLIST *h, int x )
{ SLIST *p, *q, *s;
    s=(SLIST *)malloc(sizeof(SLIST));
    s->data= ____ ; //
    q=h;
    p=h->next;
    while(p!=NULL && p->data <x) {
        q= ____ ; //
        p=p->next;
    }
    s->next=p;
    q->next= ____ ; //
}
```

---

## 第 70 题
下列给定程序中，函数fun的功能是：将形参a所指数组中的前半部分元素中的值与后半部分元素中的值对换。形参n中存放数组中数据的个数，若n为奇数，则中间的元素不动。
例如，若所指数组中的数据为：1、2、3、4、5、6、7、8、9，则调换后为：6、7、8、9、5、1、2、3、4。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(int a[], int n)
{ int i, t, p;
    p = (n%2==0)?n/2:n/2+ ____ ; //
    for (i=0; i<n/2; i++)
    {
        t=a[i];
        a[i] = a[p+ ____ ]; //
        ____ = t; //
    }
}
```

---

## 第 71 题
下列给定程序中，函数fun的功能是：从形参ss所指字符串数组中，删除所有串长超过k的字符串。函数返回剩余字符串的个数。ss所指字符串数组中共有N个字符串，且串长小于M。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
int fun(char (*ss)[M], int k)
{
    int i, j = 0, len;
    for (i = 0; i < ____; i++)
    {
        len = strlen(ss[i]);
        if (len <= ____)
            strcpy(ss[j++], ____);
    }
    return j;
}
```

---

## 第 73 题
用筛选法可得到2～n(n≤10000)之间的所有素数，方法是：首先从素数2开始，将所有2的倍数的数从数组中删去(将数组中相应位置的值置成0)，接着从数组中找到下一个非0数，并从数组中删去该数的所有倍数，依此类推，直到数组的结尾为止。这样会得到一个素数序列：2,3,5,7,11,13,17,19,23,...。函数fun的功能是：用筛选法找出所有小于等于n的素数，并统计素数的个数作为函数值返回。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
int fun(int n)
{
    int a[10000], i, j, count = 0;
    
    for (i = 2; i <= n; i++)
        a[i] = i;
    
    i = 2;
    while (i < n)
    {
        for (j = a[i] * 2; j <= n; j += ____)
            a[j] = 0;
        
        i++;
        while (____ == 0)
            i++;
    }
    
    printf("\nThe prime number between 2 to %d\n", n);
    for (i = 2; i <= n; i++)
    {
        if (a[i] != ____)
        {
            count++;
            printf(count % 15 ? "%5d" : "\n%5d", a[i]);
        }
    }
    
    return count;
}
```

---

## 第 74 题
甲乙丙丁4人同时开始放鞭炮，甲每隔t1秒放一次，乙每隔t2秒放一次，丙每隔t3秒放一次，丁每隔t4秒放一次。每人各放n次。函数fun的功能是根据形参，计算总共能听到多少次鞭炮声，只算一次响声，第一次响声是在第0秒。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
#define OK(i, t, n) ((____%t==0) && (i/t<n))
int fun(int t1, int t2, int t3, int t4, int n)
{
    int count, t, maxt = t1;
    
    if (maxt < t2) maxt = t2;
    if (maxt < t3) maxt = t3;
    if (maxt < t4) maxt = t4;
    
    count = 1;
    for (t = 1; t < maxt * (n - 1); ____)
    {
        if (OK(t, t1, n) || OK(t, t2, n) || OK(t, t3, n) || OK(t, t4, n))
            count++;
    }
    
    return ____;
}
```

---

## 第 75 题
函数fun的功能是：从三个形参a、b、c中找出中间的数，并作为函数值返回。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
int fun(int a, int b, int c)
{
    int t;
    
    t = (a > b) ? (b > c ? b : (a > c ? c : ____)) : ((a > c) ? ____ : ((b > c) ? c : ____));
    
    return t;
}
```

---

## 第 76 题
给定程序中，函数fun的功能是：对形参ss所指字符串数组中的M个字符串按长度由短到长进行排序。ss所指字符串数组中共有M个字符串，且串长小于N。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char (*ss)[N])
{ int i, j, k, n[M];    char t[N];
    for(i=0; i<M; i++) n[i]=strlen(ss[i]);
    for(i=0; i<M-1; i++)
    {
        k=i;
        for(j=____; j<M; j++) //
            if(n[k]>n[j]) ____; //
        if(k!=i)
        {
            strcpy(t,ss[i]);
            strcpy(ss[i],ss[k]);
            strcpy(ss[k],____); //
            n[k]=n[i];
        }
    }
}
```

---

## 第 77 题
给定程序中，函数fun的功能是：求出形参ss所指字符串数组中最长字符串的长度，其余字符串左用字符*补齐，使其与最长的字符串等长。字符串数组中共有M个字符串，且串长小于N。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char (*ss)[N])
{ int i, j, k=0, n, m, len;
    for(i=0; i<M; i++)
    {
        len=strlen(ss[i]);
        if(i==0) n=len;
        if(len>n) {
            n=len; 
			____=i; //
        }
    }
    for(i=0; i<M; i++)
        if(i!=k)
        {
            m=n;
            len=strlen(ss[i]);
            for(j=____; j>=0; j--) //
                ss[i][m--]=ss[i][j];
            for(j=0; j<n-len; j++)
                ____='*'; //
        }
}
```

---

## 第 78 题
给定程序中，函数fun的功能是：求出形参ss所指字符串数组中最长字符串的长度，将其余字符串右边用字符*补齐，使其与最长的字符串等长。ss所指字符串数组中共有M个字符串，且串长小于N。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char (*ss)[N])
{ int i, j, n, len=0;
    for(i=0; i<M; i++)
    {
        len=strlen(ss[i]);
        if(i==0) n=len;
        if(len>n)n=len;
    }
    for(i=0; i<M; i++) {
        n=strlen(____); //计算每个字符串的长度
        for(j=0; j<len-n; j++)
            ss[i][____]='*'; //
        ss[i][n+j+____]='\0'; //
    }
}
```

---

## 第 79 题
给定程序中，函数fun的功能是：求出形参ss所指字符串数组中长度最长的字符串所在的行下标，作为函数值返回，并把其串长放在形参n所指变量中。ss所指字符串数组中共有M个字符串，且串长<N。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
int fun(char (*ss)____, int *n) //
{ int i, k=0, len=0;
    for(i=0; i<M; i++)
    {
        len=strlen(ss[i]);
        if(i==0) *n=____; //
        if(len>*n) {
            ____; //
            k=i;
        }
    }
    return(k);
}
```

---

## 第 81 题
函数fun的功能是进行字母转换。若形参ch中是小写英文字母，则转换成对应的大写英文字母；若ch是大写英文字母，则转换成对应的小写英文字母；若是其他字符则保持不变，并将转换后的结果作为函数值返回。

请在程序的下划线处填入正确的内容并把下划线删除，使程序得出正确的结果。

### 程序
```c
char fun(char ch)
{
    if ((ch >= 'a') ____ (ch <= 'z'))
        return ch - 'a' + 'A';
    if (isupper(ch))
        return ch + 'a' - ____;
    return ____;
}
```

---

## 第 82 题
函数fun的功能是：计算f(x)=1-x^2/2!+x^4/4!-x^6/6!+...+(-1)^(n-1)x^(2n-2)/(2n-2)!+(-1)^n x^(2n)/(2n)!的前n项之和。若x=2.5，n=15时，函数值为1.917914。

请在程序的下划线处填入正确的内容并把下划线删除，使程序得出正确的结果。

### 程序
```c
double fun(double x, int n)
{
    double f, t; int i;
    f = ____;
    t = -1;
    for (i = 1; i < n; i++)
    {
        t *= (____) * x / i;
        f += ____;
    }
    return f;
}
```

---

## 第 83 题
函数fun的功能是：计算f(x)=1+x-x^2/2!+x^3/3!-x^4/4!+...+(-1)^(n-1)x^(n-1)/(n-1)!+(-1)^n x^n/n!直到|t| < 1e-6。若x=2.5，函数值为1.917915。

请在程序的下划线处填入正确的内容并把下划线删除，使程序得出正确的结果。

### 程序
```c
double fun(double x)
{
    double f, t; int n;
    f = 1.0 + x;
    t = ____;
    n = 1;
    do
    {
        n++;
        t *= (-1.0) * x / ____;
        f += t;
    } while (____ >= 1e-6);
    return f;
}
```

---

## 第 84 题
函数fun的功能是：计算f(x)=1+x+x^2/2!+x^3/3!+...+x^n/n!的前n项之和。若x=2.5，n=15时，函数值为12.182494。

请在程序的下划线处填入正确的内容并把下划线删除，使程序得出正确的结果。

### 程序
```c
double fun(double x, int n)
{
    double f, t; int i;
    f = 1.0;
    t = ____;
    for (i = ____; i < n; i++)
    {
        t *= x / ____;
        f += t;
    }
    return f;
}
```

---

## 第 85 题
函数fun的功能是：计算公式直到某项绝对值小于10^{-6}。

请在程序的下划线处填入正确的内容，使程序得出正确的结果。

### 程序
```c
double fun(double x)
{
     double f, t; int n;
    {
        f = 1.0+______;
        t = x;
        n = 1;
        do
        n++;
        t *= x/______;
        f += ______;
        } while (fabs(t) >= 1e‐6);
        return f;
    }
```

---

## 第 86 题
函数fun的功能是：统计所有小于等于n(n≥2)的素数的个数，素数的个数作为函数值返回。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
int fun(int n)
{ int i,j, count=0;
    printf("\nThe prime number between 3 to %d\n", n);
    for(i=3; i<=n; i++) {
        for (____; j<i; j++) //
            if (____%j == 0) //
                break;
        if (____>=i) //
        { count++; printf( count%15? "%5d":"\n%5d",i); }
    }
    return count;
}
```

---

## 第 87 题
函数fun的功能是：统计形参n的各位上出现数字1、2、3的次数，并用外部全局变量c1、c2、c3返回主函数。
例如，当n=123114350时，结果应为c1=3 c2=1 c3=2。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(long n)
{
    c1=c2=c3=0;
    while(n)
    {
        switch(____) //
        {
        case 1:
            c1++;
			____; //
        case 2:
            c2++;
			____; //
        case 3:
            c3++;
        }
        n/=10;
    }
}
```

---

## 第 88 题
下列给定程序中，函数fun的功能是：把形参s所指字符串中下标为奇数的字符右移到一个奇数位置，最右边被移出字符串的字符再放回到第一个奇数位置，下标为偶数的字符不动，字符串的长度大于等于2。
例如，形参s所指字符串为"abcdefg"，执行结果为"aebfcgd"。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char *s)
{ int i, n, k;    char c;
    n=0;
    for(i=0; s[i]!='\0'; i++) n++;
    if(n%2==0) k=____ ; //
    else
        k=n-2;
    c=____ ; //
    for(i=k-2; i>=1; i-=2) s[i+2]=s[i];
    s[1]=____//
}
```

---

## 第 89 题
下列给定程序中，函数fun的功能是：在形参ss所指字符串数组中查找与形参t所指字符串相同的串，找到后返回该串在字符串数组中的位置(下标值)。若未找到则返回-1。ss所指字符串数组中共有N个内容不同的字符串，且串长小于M。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
int fun(char (*ss)[M],char *t)
{ int i;
    for(i=0; i<____ ; i++) //
    /**********found**********/
        if(strcmp(ss[i],t)==0) return ____ ; //
    return -1;
}
main()
{ ...
    n=fun(ch,t);
    if(n== ____) printf("\nDon't found!\n"); //
    else    printf("\nthe position is  %d .\n",n);
}
```

---

## 第 90 题
下列给定程序中已建立了一个带头结点的单向链表，在main函数中将多次调用fun函数，每调用一次，输出链表尾部结点中的数据，并释放该结点，使链表缩短。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun( SLIST *p)
{ SLIST *t, *s;
    t=p->next;    s=p;
    while(t->next != NULL)
    {
        s=t;
        t=t->____; //
    }
    printf(" %d ",____); //
    s->next=NULL;
    free(____); //
}
```

---

## 第 91 题
下列给定程序中，函数fun的功能是：逆置数组元素中的值。例如，若所指数组中的数据为：1、2、3、4、5、6、7、8、9，则逆置后依次为：9、8、7、6、5、4、3、2、1。形参n给出数组中数据的个数。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(int a[], int n)
{ int i,t;
    for (i=0; i<____; i++) //
    {
        t=a[i];
        a[i] = a[n-1-____]; //
        _____ = t;
    }
}
```

---

## 第 92 题
下列给定程序的功能是：调用函数fun将指定源文件中的内容复制到指定的目标文件中，复制成功后返回1，失败时返回0。在复制的过程中，把复制的内容输出到屏幕。主函数中源文件名放在变量source中，目标文件名放在变量target中。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
int fun(char *source, char *target)
{ FILE *fs,*ft;  char ch;
    if((fs=fopen(source, ____))==NULL) //以只读的方式打开文件
        return 0;
    if((ft=fopen(target, "w"))==NULL)
        return 0;
    printf("\nThe data in file :\n");
    ch=fgetc(fs);
    while(!feof(____)) //检测是否到文件末尾
    { putchar( ch );
        fputc(ch, ____); //
        ch=fgetc(fs);
    }
    fclose(fs); fclose(ft);
    printf("\n\n");
    return 1;
}
```

---

## 第 93 题
下列给定程序中，函数fun的功能是：有N×N矩阵，根据给定的m(m≤N)值，将每行元素中的值均向右移动m个位置，左边置为0。

例如，N=3，m=2，有下列矩阵：
1 2 3
4 5 6
7 8 9
程序执行结果为：
0 0 1
0 0 4
0 0 7

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(int (*t)[N], int m)
{ int i, j;
    for(i=0; i<N; ____ ) //
    { for(j=N-1-m; j>=0; j--)
        t[i][j+____] =t[i][j]; //
      for(j=0; j<____; j++) //
        t[i][j]=0;
    }
}
```

---

## 第 94 题
程序通过定义学生结构体变量，存储学生的学号、姓名和三门课的成绩。函数fun的功能是：将形参所指结构体变量中的数据赋给函数中的结构体变量b，并修改b中的学号和姓名，最后输出修改后的数据。

例如，所给结构体变量中的学号、姓名和三门课的成绩依次是：10001、"ZhangSan"、95、80、88，则修改后输出的数据应为：10002、"LiSi"、95、80、88。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(struct student a)
{ struct student b; int i;
    b = ____; //
    b.sno = 10002;
    strcpy(____, "LiSi"); //
    printf("\nThe data after modified :\n");
    printf("\nNo: %ld Name: %s\nScores: ",b.sno, b.name);
    for (i=0; i<3; i++) printf("%6.2f ", b.____); //
    printf("\n");
}
```

---

## 第 95 题
程序通过定义学生结构体变量，存储了学生的学号、姓名和三门课的成绩。函数fun的功能是：从键盘输入若干学生数据，把他们放入一个带头结点的单向链表中，链表结点按学号从小到大排列，输入以-1结束。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void WriteText(FILE ____) //
{ char str[81];
    printf("\nEnter string with -1 to end :\n");
    gets(str);
    while(strcmp(str,"-1")!=0) {
        fputs(____,fw); fputs("\n",fw); //
        gets(str);
    }
}
void ReadText(FILE *fr)
{ char str[81];
    printf("\nRead file and output to screen :\n");
    fgets(str,81,fr);
    while (!feof(fr)) {
        printf("%s",____); //
        fgets(str,81,fr);
    }
}
```

---

## 第 96 题
下列给定程序中，函数fun的功能是：有N×N矩阵，将矩阵的外围元素顺时针旋转。操作顺序是：首先将第一行元素的值存入临时数组，然后使第一列成为第一行，最后一行成为第一列，最后一列成为最后一行，再使临时数组中的元素成为最后一列。

例如，若N=3，有下列矩阵：
1 2 3
4 5 6
7 8 9
操作后应为：
7 4 1
8 5 2
9 6 3

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(int (*t)[N])
{
    int j, r[N];
    for(j=0; j<N; j++) r[j]=t[0][j];
    for(j=0; j<N; j++)
        t[0][N-1-j]=t[j][____]; //
    for(j=0; j<N; j++)
        t[j][0]=t[N-1][j];
    for(j=N-1; j>=0; ____) //
        t[N-1][N-1-j]=t[j][N-1];
    for(j=N-1; j>=0; j--)
        t[j][N-1]=r[____];
}
```

---

## 第 97 题
下列给定程序中，函数fun的功能是：在形参ss所指字符串数组中，查找含有形参substr所指子串的所有字符串并输出，若没找到则输出相应信息。ss所指字符串数组中共有N个字符串，且串长小于M。程序中库函数strstr（a1, a2）的功能是在a1字符串中查找a2子串，若没有，函数值为0，若有，函数值为非0。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char (*ss)[M], char *substr)
{ 
    int i, find=0;
    for(i=0; i <____; i++) //
        if( strstr(ss[i], ____) != NULL ) //
        { find=1; puts(ss[i]); printf("\n"); }
    if (find==____) printf("\nDon't found!\n"); //
}
```

---

## 第 98 题
下列给定程序中，函数fun的功能是：计算一个带头结点的单向链表中各节点的数据域中数值之和，结果作为函数值返回。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
int fun( SLIST *h)
{ 
    SLIST *p;
    int s=0;
    p=h->next;
    while(p)
    {
        s+=p->____; //
        p=p->____; //
    }
    return s;
}
main()
{
    SLIST *head;
    int a[N]={12,87,32,91,16,20,48};
    head=creatlist(a);
    outlist(head); //
    printf("\nsum=%d\n",fun(____)); //
}
```

---

## 第 99 题
程序通过定义学生结构体变量，存储学生的学号、姓名和三门课的成绩，函数fun的功能是：将形参a所指结构体变量中的数据进行修改，并把修改后的地址作为函数值返回主函数，从主函数中输出修改的数据。

例如，a所指变量中的学号、姓名和三门课的成绩依次是：10001、"ZhangSan"、95、80、88，修改后输出的数据应为：10002、"LiSi"、96、81、89。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
____ fun(struct student *a) //
{ 
    int i;
    a->sno = 10002;
    strcpy(a->name, "LiSi");
    for (i=0; i<3; i++) ____ += 1; //
    return ____; //
}
```

---

## 第 100 题
下列给定程序中，函数fun的功能是：将N×N矩阵中主对角线元素与反对角线对应位置上元素的值进行交换。

例如，若N=3，有下列矩阵：
1 2 3
4 5 6
7 8 9
交换后为：
1 2 7
4 5 4
3 8 9

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(int ____, int n) //
{ 
    int i,s;
    for(____; i++) //
    { 
        s=t[i][i];
        t[i][i]=t[i][n-1-i];
        t[i][n-1-i]=____; //
    }
}
```

---

## 第 101 题
下列给定程序中，函数fun的功能是：找出100～999之间（含100和999）所有各位上的数字之和为x（x为正整数）的整数，输出符合条件的整数个数作为函数值返回。

例如，当x值为5时，100～999之间各位上的数字之和为5的整数有：104、113、122、131、140、203、212、221、230、302、311、320、401、410、500，共有15个。当x值为27时，各位数字之和为27的整数是：999，只有1个。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
int fun(int x)
{
    int n, s1, s2, s3, t;
    n=0;
    t=100;
    while(t<=____) { //
        s1=t%10; s2=(____)%10; s3=t/100; //
        if(s1+s2+s3==____) //
        { printf("%d ",t);
            n++;
        }
        t++;
    }
    return n;
}
```

---

## 第 102 题
程序通过定义学生结构体变量，存储学生的学号、姓名和三门课的成绩，函数fun的功能是：对形参b所指结构体变量中的数据进行修改，并在主函数中输出修改后的数据。

例如，b所指变量中的学号、姓名和三门课的成绩依次是：10002、"ZhangSan"、93、85、87，修改后输出的数据应为：10004、"LiJie"、93、85、87。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun( struct student *b)
{
    b____ = 10004; //
    strcpy(b____, "LiJie"); //
}
main()
{
    ...
    fun(____); //
    printf("\nThe data after modified :\n");
    printf("\nNo: %ld Name: %s\nScores: ",t.sno, t.name);
    for (i=0; i<3; i++) printf("%6.2f ", t.score[i]);
    printf("\n");
}
```

---

## 第 103 题
下列给定程序中已建立了一个带头结点的单向链表，链表中的各结点按数据域递增有序链接。函数fun的功能是：删除链表中数据域值相同的结点，使之只保留一个。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun( SLIST *h)
{
    SLIST *p, *q;
    p=h->next;
    if (p!=NULL)
    {
        q=p->next;
        while(q!=NULL)
        {
            if (p->data==q->data)
            {
                p->next=q->next;
                free(____); //
                q=p->____; //
            }
            else
            {
                p=q;
                q=q->____; //
            }
        }
    }
}
```

---

## 第 104 题
下列给定程序中，函数fun的功能是：有N×N矩阵，以主对角线为对称线，对称元素相加并将结果存放在左下三角元素中，右上三角元素置为0。

例如，若N=3，有下列矩阵：
1 2 3
4 5 6
7 8 9
计算结果为：
1 0 0
6 5 0
10 14 9

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(int (*t)____) //
{
    int i, j;
    for(i=1; i<N; i++)
    {
        for(j=0; j<i; j++)
        {
            ____=t[i][j]+t[j][i]; //
            ____=0; //右上三角元素置0
        }
    }
}
```

---

## 第 105 题
程序通过定义学生结构体变量，存储学生的学号、姓名和三门课的成绩，所有学生数据均以二进制方式输出到student.dat文件中。函数fun的功能是：从文件中找出指定学号的学生数据，读入该学生数据，对该学生的分数进行修改，使每门课的分数加5分，修改后重新写回文件中该学生的数据，即用该学生的新数据覆盖原数据，其它学生数据指定不变；若找不到，则不做任何操作。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun(char *filename, long sno)
{
    FILE *fp;
    STU n;
    int i;
    fp = fopen(filename,"rb+");
    while (!feof(____)) //
    {
        fread(&n, sizeof(STU), 1, fp);
        if (n.sno____sno) brseak; //
    }
    if (!feof(fp))
    {
        for (i=0; i<3; i++) n.score[i] += 5;
        fseek(____, -(long)sizeof(STU), SEEK_CUR); //
        fwrite(&n, sizeof(STU), 1, fp);
    }
    fclose(fp);
}
```

---

## 第 106 题
函数isleep的功能是判断所给的年、月、日，计算该日是这一年的第几天，并作为函数值返回，其中函数isleep用来判断某一年是否为闰年。

例如，若输入：2008 3 1，则程序输出：2008年3月1日是该年的第61天。

请在程序的下划线处填入正确的内容，并把下划线删除，使程序得出正确的结果。

### 程序
```c
int isleap(int year)
{ int leap;
    leap= (year%4==0 && year%100!=0 || year%400==0);
    return ____; //
}
int fun(int year, int month, int day)
{ int table[13]={0,31,28,31,30,31,30,31,31,30,31,30,31};
    int days=0, i;
    for(i=1; i<month; i++)
        days=days+ table[i];
    days=days+ ____ ; //
    if(isleap(year) && month>2 )
        days=days+____; //如果是闰年并且累加月份大于2
    return days;
}
```

---

## 第 107 题
函数fun的功能是：在有n个元素的结构体数组std中，查找有不及格科目的学生，找到后输出学生的学号，函数的返回值是有不及格科目的学生人数。

例如，主函数中给出了4名学生的数据，则程序运行的结果为：
学号：N1002 学号：N1006
共有2位学生有不及格科目

请在程序的下划线处填入正确的内容，并把下划线删除，使程序得出正确的结果。

### 程序
```c
typedef struct
{ char num[8];
    double score[2];
} ____ ; //
int fun(STU std[] , int n )
{ int i, k=0;
    for(i=0; i<n; i++)
        if (std[i].score[0]<60||____ ) //
        {
            printf("学号：%s ",std[i].num);
            k++;
        }
    return ____ ; //
}
```

---

## 第 108 题
函数fun的功能是：将一副扑克牌编号为1,2,…,53,54，以某种特定的方式洗牌，这种方式是将这副牌分成两半，然后将它们交叉，开始始终保持编号为1的牌在最上方，譬如第一次这样洗牌后的结果为：1,28,2,29,…26,53,27,54，两次洗牌后的结果为：1,14,28,42,2,15,29,43,…,27,41,54。

程序的功能是，输出经过两次这样洗牌后的结果。

请在程序的下划线处填入正确的内容，并把下划线删除，使程序得出正确的结果。

### 程序
```c
void fun( int a[55], int n )
{ int i, k ;
    int ____; //
    for (i=0; i<n; i++)
    {
        for (k=1; k<=27; k++)
        {
            b[2*k-1] = a[k];
            b[ ____* k ] = a[k+27]; //
        }
    }
    for (k=1; k<=54; k++)
        a[k]=____; //
}
```

---

## 第 109 题
给定程序中，函数fun的作用是：不断从标准输入整数，由变量a统计大于0的个数，变量b统计小于0的个数，当输入0时结束输入，并通过形参pa和pb把统计的数据传回主函数进行输出。

请在程序的下划线处填入正确的内容并将下划线删除，使程序得出正确的结果。

### 程序
```c
void fun( int *px, int *py)
{ int ____ ; //
    scanf("%d", &k );
    while ____ //
    {
        if (k>0) a++;
        if(k<0) b++;
        ____; //
    }
    *px=a; 
	*py=b;
}
```
