#include <stdio.h>
#include <string.h>

int main() {
    /*
    char *p = "abc"; // 字符串常量不能被修改
    char q[] = "abc";
    printf("%s\n", p);
    printf("%s\n", q);
    p = "def"; // 这里是正确的，这里是改变指针指向，不是修改字符串常量
    // *p = 'd'; // 这里是错误的，字符串常量不能被修改
    *q = 'd'; // 或者 q[0] = 'd';
    printf("%s\n", p);
    printf("%s\n", q);
    */


    return 0;
}
